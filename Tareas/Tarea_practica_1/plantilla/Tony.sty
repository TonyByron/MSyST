% ============================================================
% Compatibilidad: archivo histórico de la plantilla original
% ============================================================
% Las definiciones funcionales permanecen en tony.sty.
% Este archivo se conserva para no romper documentos existentes.

% Paquetes históricos que no interfieren con R Markdown/XeLaTeX
\usepackage{amsmath,amsthm,amscd,amsfonts,amssymb,dsfont}
\usepackage{titleref}
\usepackage{upgreek}

\let\vacio\varnothing

% Si un trabajo anterior depende de comandos definidos en el
% Tony.sty original, se mantienen aquí para compatibilidad.
\newcommand{\epscompat}{\varepsilon}
